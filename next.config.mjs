/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "zealous-bee-636.convex.cloud"
      }
    ]
  }
};

export default nextConfig;
